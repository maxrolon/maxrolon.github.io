<section class="project w1 w{{width}}_m float-l half-gutter flex-cell">
  <div class="project__table bg-c-g1">
    <div class="project__inner align-c cw">
      <h3 class="underline"><a href="{{url}}">New Project</a></h3>
      <h1 class="project__title">{{title}}</h1>
      <i class="o-diagonal"></i>
      <p>{{contents}}</p>
      <p>Check out this <a href="{{url}}">Case Study</a></p>
    </div>
  </div>
</section>
